/** The reference zone for "today" — every date the field route displays or
 * matches against is pinned here, so the page reads the same regardless of
 * where the server process runs. Vercel's Node runtime defaults to UTC. */
export const TIME_ZONE = 'Europe/London'
